<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Assignment1.Views.ItemDetailPage"
             Title="{Binding Title}">

  <Grid>        <StackLayout Spacing="20" Padding="15">
        <WebView Source="{Binding Uri}" Navigated="WebView_Navigated" Navigating="WebView_Navigating" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand"/>
    </StackLayout>
        <ActivityIndicator IsRunning="{Binding IsBusy}" HorizontalOptions="Center" VerticalOptions="Center"/>
   </Grid>
    
</ContentPage>